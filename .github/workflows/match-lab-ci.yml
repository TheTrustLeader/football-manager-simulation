name: Match Lab CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install dependencies
        run: npm install

      - name: Build and type-check
        run: npm run build

      - name: Print deterministic golden-output hash
        run: npm run golden:print

      - name: Run tuning-seed CI evidence gate
        id: evidence
        continue-on-error: true
        run: npm run evidence -- 20000 tuning evidence/match-lab-evidence.json

      - name: Run 500,000-seed game-state evidence
        id: gamestate
        continue-on-error: true
        run: npm run game-state:evidence -- 500000 evidence/game-state-full-evidence.json

      - name: Rerun REVIEW-002 full tuning evidence
        id: review002
        continue-on-error: true
        run: npm run review002:evidence -- 500000 evidence/review-002-full-evidence.json

      - name: Run behavioural and invariant tests
        id: tests
        continue-on-error: true
        run: npm test

      - name: Upload Match Lab evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: match-lab-evidence
          path: evidence/*.json
          if-no-files-found: error
          retention-days: 90

      - name: Enforce evidence and test gates
        if: always()
        run: |
          if [ "${{ steps.evidence.outcome }}" != "success" ] || [ "${{ steps.gamestate.outcome }}" != "success" ] || [ "${{ steps.review002.outcome }}" != "success" ] || [ "${{ steps.tests.outcome }}" != "success" ]; then
            echo "Match Lab gate failed: evidence=${{ steps.evidence.outcome }}, gamestate=${{ steps.gamestate.outcome }}, review002=${{ steps.review002.outcome }}, tests=${{ steps.tests.outcome }}"
            exit 1
          fi
